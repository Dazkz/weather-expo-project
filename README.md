# Сделано через expo-cli

Приложение показывающая тепературу и погоду, ссылаясь на выбранный в списке город.

git clone https://github.com/Dazkz/weather-expo-project.git

## Available Scripts

In the project directory, you can run:

### `npm start`

### `npm run web`
